//this stuff will gonna print content the every child it belong to 
// and here it how it goes div.innerText
// const div = document.querySelector("h2");
// div.innerText = div.innerText + "is being learn by me"
// console.dir(div);

// let change = document.querySelectorAll(".box");
// //this is how stuff are change by their index number
// change[0].innerText = "this is box1"; 
// change[1].innerText = "this is box2"; 
// change[2].innerText = "this is box3";

// it will print tag + content = which mean entire element
// here how it goes syntatically
// div.innerHTML

// this thing will to manipulate or change the content or tag of html
//basically how its how done 
//div.textCONTENT = <div>this will change older content and will be replace with this new one<\div> 
//we can change the text and tags with innerTEXT and innerHTML
// textCONTENT used to display the hidden content

let ad = document.querySelectorAll(".box");
ad.addEvent
